const functions = {
    add: (num1, num2) => num1 + num2,
    add: function (x1, x2) {
        return x1 + x2
    },
    sub: (y1, y2) => y1 -y2,
    metalx: (z1, z2, z3) => z1 * z2 * z3,
    checkVal: (x) => x,
}


module.exports = functions;